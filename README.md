this repository will have the following files->
1-> transformer.py (contains the model)
2-> transformer.ipynb (also containts the code of model and trained parameters of the model training will be done in this notebook)
3-> inference.py ( in this file we will use the trained parameters and the model)
4-> app.py ( this is a fastapi server containing prerendered HTML that has several APIs for model usage as an application)
